# Copy to .env for Docker Compose. Never commit a real .env file.
POSTGRES_USER=tesla_pool
POSTGRES_PASSWORD=choose-a-long-local-password
POSTGRES_DB=tesla_pool
POSTGRES_PORT=5432
API_PORT=3001
WEB_PORT=3000
NODE_ENV=development
APP_ORIGIN=http://localhost:3000
# Set a strong local-only password before starting Compose. Never commit its value.
AUTH_DEMO_PASSWORD=
